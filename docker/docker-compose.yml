version: "3.8"
services:
    icm-web-2:
        image: proget.icm.com/docker/icm-website:latest
        container_name: next-icm-com-2
        restart: always
        ports:
            - "5002:8080"
        # environment:
        # volumes:
            # - /etc/nginx/content/:/usr/share/nginx/
            # - /etc/nginx/config/:/etc/nginx/
            # - /etc/nginx/ssl/:/etc/letsencrypt/live/
            # - /etc/nginx/geoip/:/usr/share/GeoIP/
        networks:
            - next-icm-net-2
networks:
    next-icm-net-2:
